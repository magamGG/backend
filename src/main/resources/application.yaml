logging:
  level:
    org.hibernate.SQL: DEBUG
    org.hibernate.orm.jdbc.bind: TRACE
    com.kh.magamGG.global.security.JwtAuthenticationFilter: INFO
    com.kh.magamGG.domain.notification: INFO

spring:
  application:
    name: magamgg

  datasource:
    url: jdbc:mysql://localhost:3306/magamgg?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: magam_user
    password: asdf1234!
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      maximum-pool-size: 30
      minimum-idle: 5
      connection-timeout: 60000
      idle-timeout: 300000
      max-lifetime: 1800000

  servlet:
    multipart:
      max-file-size: 20MB
      max-request-size: 20MB

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  # 캐싱 설정 (최적화)
  cache:
    type: simple
    cache-names: leaveBalance, agencyInfo

  mail:
    host: smtp.gmail.com
    port: 587
    username: magamgglocalservice@gmail.com
    password: gniy mzoy ewjw lqna  # 앱 비밀번호
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
          connectiontimeout: 5000
          timeout: 5000
          writetimeout: 5000

jwt:
  access-secret: ${JWT_ACCESS_SECRET:dev-access-secret-key-12345678901234567890123456789012}
  refresh-secret: ${JWT_REFRESH_SECRET:dev-refresh-secret-key-98765432109876543210987654321098}
  access-expiration: 900000      # 15분 (밀리초)
  refresh-expiration: 604800000  # 7일 (밀리초)

server:
  port: 8888

  servlet:
    encoding:
      charset: UTF-8
      enabled: true
      force: true

file:
  upload-dir: uploads

# Ollama AI 설정 (설치된 모델 사용: ollama list 확인)
# - qwen2.5:7b: 4.3GB 필요 (메모리 부족 시 사용 불가)
# - qwen2.5:1.5b: ~1.5GB (ollama pull qwen2.5:1.5b)
# - qwen2:0.5b: ~500MB (ollama pull qwen2:0.5b)
ollama:
  base-url: http://localhost:11434
  model: qwen2:0.5b   # 가장 작음(~500MB). 없으면: ollama pull qwen2:0.5b
  timeout: 120000