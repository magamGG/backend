logging:
  level:
    org.hibernate.SQL: DEBUG
    org.hibernate.orm.jdbc.bind: TRACE

spring:
  application:
    name: magamgg

  datasource:
    url: jdbc:mysql://localhost:3306/magamgg?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: magam_user
    password: asdf1234!
    driver-class-name: com.mysql.cj.jdbc.Driver

  servlet:
    multipart:
      max-file-size: 20MB
      max-request-size: 20MB

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  # 캐싱 설정 (최적화)
  cache:
    type: simple
    cache-names: leaveBalance, agencyInfo

jwt:
  access-secret: XEnXUlYA+RAWHP1MDdQHmbq6xKiFP3rWABp01TxYGWc=
  refresh-secret: YFnYVmZB+SBXIQ2NEeRncq7yLjGQ4sXBCq12UyZHXd=
  access-expiration: 900000      # 15분 (밀리초)
  refresh-expiration: 604800000  # 7일 (밀리초)

server:
  port: 8888

  servlet:
    encoding:
      charset: UTF-8
      enabled: true
      force: true

mail:
  host: smtp.gmail.com
  port: 587
  username: magamgglocalservice@gmail.com
  password: gniy mzoy ewjw lqna  # 앱 비밀번호
  properties:
    mail:
      smtp:
        auth: true
        starttls:
          enable: true
          required: true
        connectiontimeout: 5000
        timeout: 5000
        writetimeout: 5000

file:
  upload-dir: uploads

# Ollama AI 설정
ollama:
  base-url: http://localhost:11434
  model: qwen2.5:7b
  timeout: 120000