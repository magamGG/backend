logging:
  level:
    org.hibernate.SQL: DEBUG
    org.hibernate.orm.jdbc.bind: TRACE
    com.kh.magamGG.global.security.JwtAuthenticationFilter: INFO
    com.kh.magamGG.domain.notification: INFO
    com.kh.magamGG.global.security: DEBUG  # JWT 관련 디버그 로그 활성화

spring:
  application:
    name: magamgg

  datasource:
    url: jdbc:mysql://localhost:3306/magamgg?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: magam_user
    password: asdf1234!
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      maximum-pool-size: 30
      minimum-idle: 5
      connection-timeout: 60000
      idle-timeout: 300000
      max-lifetime: 1800000

  servlet:
    multipart:
      max-file-size: 20MB
      max-request-size: 20MB

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  sql:
    init:
      mode: never  # 스키마 파일 실행하지 않음

  # 캐싱 설정 (최적화)
  cache:
    type: simple
    cache-names: leaveBalance, agencyInfo

  mail:
    host: smtp.gmail.com
    port: 587
    username: magamgglocalservice@gmail.com
    password: gniy mzoy ewjw lqna  # 앱 비밀번호
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
          connectiontimeout: 5000
          timeout: 5000
          writetimeout: 5000

jwt:
  access-secret: ${JWT_ACCESS_SECRET:dev-access-secret-key-12345678901234567890123456789012}
  refresh-secret: ${JWT_REFRESH_SECRET:dev-refresh-secret-key-98765432109876543210987654321098}
  access-expiration: 900000      # 15분 (밀리초)
  refresh-expiration: 604800000  # 7일 (밀리초)

server:
  port: 8888

  servlet:
    encoding:
      charset: UTF-8
      enabled: true
      force: true

file:
  upload-dir: uploads

# 공공데이터 API 설정
public-data:
  holiday:
    api-key: pFg7%2B9ZuBDORVUL2Qy9DA9FffkmHzyemVbSPIN0trHBQjjgZp%2FfruTLqscT7swt3tdGf1Wi0tjZ6Lehba3YeHg%3D%3D
    base-url: http://apis.data.go.kr/B090041/openapi/service/SpcdeInfoService
    data-dir: holidays  # uploads/holidays 디렉토리에 저장